package com.weixin.command;

public class Account {
	private AccountInfo info;
	private AccountConfig config;

	public Account(AccountInfo info, AccountConfig config) {
		this.info = info;
		this.config = config;
	}

	
}
